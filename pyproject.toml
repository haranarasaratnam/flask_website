[project]
name = "flask-website"
version = "0.1.0"
description = "Personal Flask website with technical content sections (estimation, control, coding) and portfolio."
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
]
